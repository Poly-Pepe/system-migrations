pipeline {
    agent { label 'test-stand' }

    triggers { pollSCM('* * * * *') }
    options { timestamps () }

    stages {
        stage('Show Workspace') {
            steps {
                echo "Workspace directory: ${env.WORKSPACE}"
            }
        }
    }
}
